"""LeakWatch — a responsible-disclosure secret-leak monitor.

Detects secrets accidentally pushed to public GitHub, records a *redacted*
finding for review, and never uses, stores, or reuses the raw secret.
"""

__version__ = "0.1.0"
